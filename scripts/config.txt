[GLOBALS]
assetsDirname = assets
assetDatabaseFilepath = ${assetsDirname}/data.db
assetConfigFilepath =${assetsDirname}/config.ini

[ASSET.CONFIG]
graphics.WindowWidth = 640
graphics.WindowHeight = 480
graphics.WindowTitle = "cpptest"
graphics.MaxInstanceCount = 1000
controls.Enabled = 1
settings.StartScene = "triangles"

[ASSET.DB]
types = "BYTE:uint8_t, INT:uint32_t, FLOAT:float, VEC2:vec2, VEC3:vec3, VEC4:vec4, MAT4:mat4, TEXT:UT_string *, KEY:hash_t"
image = "key KEY, width INT, height INT, depth INT, channels INT, data BYTE_ARRAY"
sampler = "key KEY, magFilter INT, minFilter INT, addressWrapU INT, addressWrapV INT"
texture = "key KEY, image KEY, sampler KEY"
material = "key KEY, baseColorFactor VEC4, metallicFactor FLOAT, roughnessFactor FLOAT, baseColorTexture KEY, metallicRoughnessTexture KEY"
vertexAttribute = "key KEY, valuesInt INT_ARRAY, valuesVec2 VEC2_ARRAY, valuesVec3 VEC3_ARRAY"
primitive = "key KEY, material KEY, topology INT, indices KEY, positions KEY, normals KEY, colors KEY, texCoords KEY"
mesh = "key KEY, primitives KEY_ARRAY"
object = "key KEY, transform MAT4, mesh KEY, camera KEY, children KEY_ARRAY"
camera = "key KEY, type INT, fovY FLOAT, aspectRatio FLOAT, nearZ FLOAT, farZ FLOAT"
scene = "key KEY, name TEXT, objects KEY_ARRAY, cameras KEY_ARRAY"
